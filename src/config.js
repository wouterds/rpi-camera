import styles from 'styles/core.css';

const Config = {
  twemoji: {
    folder: 'svg',
    ext: '.svg',
    className: styles.emoji,
  },
  staticFolder: 'public',
  httpPort: 3000,
};

module.exports = Config;
export default Config;
